import { Deck } from './_deck';
import { RandomGenerator } from '../shuffle';
export interface PinochelDeckOpts {
    rng?: RandomGenerator;
}
export declare class PinochelDeck extends Deck {
    constructor(opts?: PinochelDeckOpts);
    private static generateCards;
}
