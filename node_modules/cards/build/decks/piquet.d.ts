import { Deck } from './_deck';
import { RandomGenerator } from '../shuffle';
export interface PiquetDeckOpts {
    rng?: RandomGenerator;
}
export declare class PiquetDeck extends Deck {
    constructor(opts?: PiquetDeckOpts);
    private static generateCards;
}
