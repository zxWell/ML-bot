import { Deck } from './_deck';
import { RandomGenerator } from '../shuffle';
export interface EuchreDeckOpts {
    rng?: RandomGenerator;
}
export declare class EuchreDeck extends Deck {
    constructor(opts?: EuchreDeckOpts);
    private static generateCards;
}
